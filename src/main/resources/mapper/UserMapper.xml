<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myproject.catchtable.mapper.UserMapper">

	<!-- 기본 쿼리들 -->
	<select id="findById" parameterType="int"
		resultType="com.myproject.catchtable.model.User">
		SELECT * FROM users WHERE user_id = #{userId}
	</select>

	<select id="findByEmail" parameterType="string"
		resultType="com.myproject.catchtable.model.User">
		SELECT * FROM users WHERE email = #{email}
	</select>

	<select id="findUsersByType" parameterType="string"
		resultType="com.myproject.catchtable.model.User">
		SELECT * FROM users WHERE user_type = #{userType} ORDER BY
		created_at DESC
	</select>

	<select id="findAllUsers"
		resultType="com.myproject.catchtable.model.User">
		SELECT * FROM users ORDER BY created_at DESC
	</select>

	<select id="findPendingBusinessUsers"
		resultType="com.myproject.catchtable.model.User">
		SELECT * FROM users
		WHERE user_type = 'BUSINESS' AND
		approval_status = 'PENDING'
		ORDER BY created_at ASC
	</select>

	<insert id="insertUser"
		parameterType="com.myproject.catchtable.model.User">
		INSERT INTO users (
		email, password, name, phone,
		birth_date, gender, user_type,
		approval_status,
		business_license_number, business_license_image,
		reservation_count,
		created_at, updated_at
		) VALUES (
		#{email}, #{password}, #{name},
		#{phone}, #{birthDate}, #{gender}, #{userType},
		#{approvalStatus},
		#{businessLicenseNumber}, #{businessLicenseImage},
		#{reservationCount}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
		)
	</insert>

	<update id="updateUser"
		parameterType="com.myproject.catchtable.model.User">
		UPDATE users SET
		email = #{email},
		name = #{name},
		phone =
		#{phone},
		birth_date = #{birthDate},
		gender = #{gender},
		updated_at =
		CURRENT_TIMESTAMP
		WHERE user_id = #{userId}
	</update>

	<update id="updatePassword" parameterType="map">
		UPDATE users
		SET password = #{newPassword},
		updated_at = NOW()
		WHERE user_id = #{userId}
	</update>

	<update id="updateApprovalStatus">
		UPDATE users SET
		approval_status = #{approvalStatus},
		rejection_reason = #{rejectionReason},
		updated_at = CURRENT_TIMESTAMP
		WHERE user_id = #{userId}
	</update>

	<update id="incrementReservationCount" parameterType="int">
		UPDATE
		users SET
		reservation_count = reservation_count + 1,
		updated_at =
		CURRENT_TIMESTAMP
		WHERE user_id = #{userId}
	</update>

	<delete id="deleteUser" parameterType="int">
		DELETE FROM users WHERE
		user_id = #{userId}
	</delete>

	<select id="existsByEmail" parameterType="string"
		resultType="boolean">
		SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
	</select>

	<select id="existsByPhone" parameterType="string"
		resultType="boolean">
		SELECT COUNT(*) > 0 FROM users WHERE phone = #{phone}
	</select>

</mapper>